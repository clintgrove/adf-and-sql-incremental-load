parameters:
  ServiceConnection: string
  SubscriptionId: string
  ResourceGroupName: string
  Location: string
  TemplateFilePath: string
  TemplateParamFilePath: string
  ServiceReleased: string
  sqlServerPasswordDA: string
  doSql: false 
  

steps:

# - task: AzurePowerShell@5
#   inputs:
#     azureSubscription: ${{ parameters.ServiceConnection }}
#     ScriptType: 'InlineScript'
#     Inline: |
#       $deploymentName1 = Get-AzResourceGroupDeployment -ResourceGroupName 'lz-ghub-test-uks-rg-01'
#       $deploymentName2 = $deploymentName1.DeploymentName[0]
#       $deployment = Get-AzResourceGroupDeployment -ResourceGroupName 'lz-ghub-test-uks-rg-01' -Name $deploymentName2
#       $sqlServerName = $deployment.Outputs.sqlServerName.Value
#       Write-Host "##vso[task.setvariable variable=sqlServerName;isOutput=true]$sqlServerName"
#     azurePowerShellVersion: 'LatestVersion'
#   displayName: 'Set SqlServerName output variable'


- ${{ if eq(parameters.doSql, 'true') }}:
  - task: AzureResourceManagerTemplateDeployment@3
    #condition: and(succeed(), eq('${{parameters.doSql}}', 'true'))
    displayName: "Create ${{ parameters.ServiceReleased}}"
    inputs:
      deploymentScope: 'Resource Group'
      azureResourceManagerConnection: ${{ parameters.ServiceConnection }}
      subscriptionId: ${{ parameters.SubscriptionId }}
      action: 'Create Or Update Resource Group'
      resourceGroupName: ${{ parameters.ResourceGroupName }}
      location: ${{ parameters.Location }}
      templateLocation: 'Linked artifact'
      csmFile: ${{ parameters.TemplateFilePath }}
      csmParametersFile: ${{ parameters.TemplateParamFilePath }}
      deploymentMode: 'Incremental'
      overrideParameters: '-administratorLoginPassword ${{parameters.sqlServerPasswordDA}}'
      outputs: armOutput

    -script: echo sql server name is $(armOutput.sqlServerFqdn.value)

- ${{ if eq(parameters.doSql, 'false') }}:
  - task: AzureResourceManagerTemplateDeployment@3
    #condition: and(succeed(), eq('${{parameters.doSql}}', 'true'))
    displayName: "Create ${{ parameters.ServiceReleased}}"
    inputs:
      deploymentScope: 'Resource Group'
      azureResourceManagerConnection: ${{ parameters.ServiceConnection }}
      subscriptionId: ${{ parameters.SubscriptionId }}
      action: 'Create Or Update Resource Group'
      resourceGroupName: ${{ parameters.ResourceGroupName }}
      location: ${{ parameters.Location }}
      templateLocation: 'Linked artifact'
      csmFile: ${{ parameters.TemplateFilePath }}
      csmParametersFile: ${{ parameters.TemplateParamFilePath }}
      deploymentMode: 'Incremental'

